spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jichini_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: thdk5238!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

# JWT 설정 (기존 그대로 유지, 문제 없음)
jwt:
  secret: jichini-secret-key-must-be-at-least-32-characters-long
  expiration: 86400000