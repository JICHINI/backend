spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jichini_db?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: thdk5238!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

jwt:
  secret: jichini-secret-key-must-be-at-least-32-characters-long
  expiration: 86400000